node_modules/
dist/
.env
.env.local
.vapid.json
*.log
.DS_Store

# Obsidian UI state (ruido)
brain/.obsidian/workspace.json

# Stubs soltos que o Obsidian cria na raiz do brain (links nao resolvidos)
brain/*.md
!brain/_*.md
brain/*.canvas

# lock files: decisão de 82cea84 — não versionados por ora
apps/*/package-lock.json
